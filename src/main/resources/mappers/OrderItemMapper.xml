<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ordersystem.dao.OrderItemDao">
    <!-- 结果映射 -->
    <resultMap id="BaseResultMap" type="com.ordersystem.entity.OrderItem">
        <id column="item_id" property="itemId" />
        <result column="order_id" property="orderId" />
        <result column="product_id" property="productId" />
        <result column="product_name" property="productName" />
        <result column="product_price" property="productPrice" />
        <result column="quantity" property="quantity" />
        <result column="total_price" property="totalPrice" />
        <result column="create_time" property="createTime" />
        <result column="update_time" property="updateTime" />
    </resultMap>
    
    <!-- 订单明细结果映射（包含商品信息） -->
    <resultMap id="OrderItemWithProductMap" type="com.ordersystem.entity.OrderItem" extends="BaseResultMap">
        <!-- 关联商品信息 -->
        <association property="product" javaType="com.ordersystem.entity.Product">
            <id column="p_product_id" property="productId" />
            <result column="p_product_name" property="productName" />
            <result column="p_product_desc" property="productDesc" />
            <result column="p_price" property="price" />
            <result column="p_stock" property="stock" />
            <result column="p_status" property="status" />
        </association>
    </resultMap>
    
    <!-- 公共列 -->
    <sql id="Base_Column_List">
        item_id, order_id, product_id, product_name, product_price, quantity, total_price, create_time, update_time
    </sql>
    
    <!-- 添加订单明细 -->
    <insert id="insertOrderItem" parameterType="com.ordersystem.entity.OrderItem" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO order_item (
            order_id, product_id, product_name, product_price, quantity, total_price
        ) VALUES (
            #{orderId}, #{productId}, #{productName}, #{productPrice}, #{quantity}, #{totalPrice}
        )
    </insert>
    
    <!-- 批量添加订单明细 -->
    <insert id="batchInsertOrderItems" parameterType="java.util.List" useGeneratedKeys="true" keyProperty="itemId">
        INSERT INTO order_item (
            order_id, product_id, product_name, product_price, quantity, total_price
        ) VALUES 
        <foreach collection="list" item="item" separator=",">
            (
            #{item.orderId}, #{item.productId}, #{item.productName}, #{item.productPrice}, 
            #{item.quantity}, #{item.totalPrice}
            )
        </foreach>
    </insert>
    
    <!-- 根据ID删除订单明细 -->
    <delete id="deleteOrderItemById" parameterType="java.lang.Integer">
        DELETE FROM order_item WHERE item_id = #{itemId}
    </delete>
    
    <!-- 根据订单ID删除订单明细 -->
    <delete id="deleteOrderItemsByOrderId" parameterType="java.lang.Integer">
        DELETE FROM order_item WHERE order_id = #{orderId}
    </delete>
    
    <!-- 更新订单明细信息 -->
    <update id="updateOrderItem" parameterType="com.ordersystem.entity.OrderItem">
        UPDATE order_item
        <set>
            <if test="orderId != null">order_id = #{orderId},</if>
            <if test="productId != null">product_id = #{productId},</if>
            <if test="productName != null">product_name = #{productName},</if>
            <if test="productPrice != null">product_price = #{productPrice},</if>
            <if test="quantity != null">quantity = #{quantity},</if>
            <if test="totalPrice != null">total_price = #{totalPrice},</if>
        </set>
        WHERE item_id = #{itemId}
    </update>
    
    <!-- 根据ID查询订单明细 -->
    <select id="getOrderItemById" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM order_item
        WHERE item_id = #{itemId}
    </select>
    
    <!-- 根据订单ID查询订单明细 -->
    <select id="getOrderItemsByOrderId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM order_item
        WHERE order_id = #{orderId}
    </select>
    
    <!-- 根据商品ID查询订单明细 -->
    <select id="getOrderItemsByProductId" parameterType="java.lang.Integer" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List" />
        FROM order_item
        WHERE product_id = #{productId}
    </select>
    
    <!-- 查询订单明细（包含商品信息） -->
    <select id="getOrderItemsWithProduct" parameterType="java.lang.Integer" resultMap="OrderItemWithProductMap">
        SELECT 
            oi.item_id, oi.order_id, oi.product_id, oi.product_name, oi.product_price, 
            oi.quantity, oi.total_price, oi.create_time, oi.update_time,
            p.product_id as p_product_id, p.product_name as p_product_name, p.product_desc as p_product_desc, 
            p.price as p_price, p.stock as p_stock, p.status as p_status
        FROM order_item oi
        LEFT JOIN product p ON oi.product_id = p.product_id
        WHERE oi.order_id = #{orderId}
    </select>
</mapper>